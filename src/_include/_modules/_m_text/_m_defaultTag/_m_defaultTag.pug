mixin m-defaultTag(tg)
	-
		var op = {
			tag:       tg.tag ? tg.tag : "div",
			baseName:  tg.baseName ? tg.baseName : "m-defaultTag",
			className: tg.className ? tg.className : "",
			text:      tg.text ? tg.text : "ダミータイトルです"
		}

	#{op.tag}( class = op.baseName + " " + op.className )
		block